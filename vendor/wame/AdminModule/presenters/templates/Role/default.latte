{block content}
	<h1>{$siteTitle}</h1>

	{if count($roleEntity) == 0}
		<div class="lead text-muted">{_'You do not have define any roles.'}</div>
		<a class="btn btn-success" n:href=":Admin:Role:, 'id' => null">{_'Create first role'}</a>
	{else}
        {control roleGrid}
	{/if}